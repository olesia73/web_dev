# JavaScript Intro
let for vars, const for const\
Function() {body}\
string ``, you can add other variables with ${name}\
focus sets element as active\
You can change const, it's just an indicator that you should not